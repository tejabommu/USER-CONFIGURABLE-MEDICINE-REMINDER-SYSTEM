# USER-CONFIGURABLE-MEDICINE-REMINDER-SYSTEM
Medicine reminder system built using LPC2148 ARM7 microcontroller. The system uses RTC to track current time and compare it with preset medicine schedules. A 16x2 LCD displays time and date, while a keypad allows users to edit RTC and medicine timings. When the scheduled time matches, a buzzer alert notifies the user.
